{
  "kind": "collectionType",
  "collectionName": "blocked_dates",
  "info": {
    "singularName": "blocked-date",
    "pluralName": "blocked-dates",
    "displayName": "Blocked Date",
    "description": "Dates when services are unavailable (holidays, maintenance, etc.)"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "date": {
      "type": "date",
      "required": true,
      "unique": true
    },
    "reason": {
      "type": "text"
    }
  }
}
