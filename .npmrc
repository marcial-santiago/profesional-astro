# Approve postinstall scripts for these packages
# (sharp + esbuild download platform-specific native binaries)
approve-builds=true

# Avoid noisy "ignored builds" warnings without prompting
ignore-pnpmfile=true
